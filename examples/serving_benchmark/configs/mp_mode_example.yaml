# BIFROST Phase 6 optional real-serving example.
# Mode: LMCache multiprocess scaffold with BIFROST remote storage backend.
# Version-sensitive: verify LMCache server commands and config keys locally.

demo:
  mode: lmcache_mp
  route: vLLM -> LMCache -> BIFROST remote storage connector -> bifrostd
  raw_vllm_kvtransfer: false
  opt_in_required:
    cli_flag: --allow-real-vllm
    environment: BIFROST_RUN_REAL_VLLM=1

serving:
  host: 127.0.0.1
  vllm_port: 8000
  lmcache_port: 9000
  model: /path/to/local/model
  tensor_parallel_size: 1
  allow_model_downloads: false

bifrost:
  endpoint: 127.0.0.1:7420
  object_type: opaque_engine_blob
  strict_validation: true
  allow_pickle_fallback: false

lmcache:
  mode: lmcache_mp
  enable_multiprocess: true
  controller:
    host: 127.0.0.1
    port: 9000
  remote_storage_plugins:
    - bifrost
  remote_url: bifrost://127.0.0.1:7420
  extra_config:
    remote_storage_plugin.bifrost.module_path: lmcache_bifrost.adapter
    remote_storage_plugin.bifrost.class_name: BifrostConnectorAdapter
    endpoint: 127.0.0.1:7420
    timeout_seconds: 5.0
    strict_validation: true
    allow_pickle_fallback: false
    engine_name: lmcache
    integration_name: lmcache_bifrost_remote_storage
    object_type: opaque_engine_blob

processes:
  startup_order:
    - bifrost-daemon
    - lmcache.server
    - vllm serve
  readiness:
    bifrost: tcp://127.0.0.1:7420
    lmcache: tcp://127.0.0.1:9000
    vllm: http://127.0.0.1:8000/health

workload:
  name: repeated_system_prompt
  request_count: 8
  prefix_repeat_groups: 2
  max_tokens: 16
  concurrency: 1
